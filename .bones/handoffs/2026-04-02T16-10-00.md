# Session Handoff — 2026-04-02

## Changed Files
- .bones/audit.jsonl (+1)
- .bones/tasks/pyr-yay.md (+4 -1)
- .vscode/settings.json (+1)

Recent commits (no new commits this session):
- 8fcfb310c chore(bones): SRE refine pyr-yay, close out pyr-5hl session artifacts

## Tasks Open
- pyr-a56: Decompose typeEvaluator.ts (epic)
- pyr-yay: Extract Type Assignment and Compatibility functions (active, claimed by Seth)
- pyr-1lc: Extract Symbol and Declaration Resolution functions
- pyr-p1w: Extract Member Access and Descriptor functions
- pyr-8b1: Extract Call Validation and Overload Resolution functions
- pyr-742: TypeScript diagnostics found during pyr-wru (bug, P3)

## Tasks Ready
- pyr-yay: Extract Type Assignment and Compatibility functions (P2, claimed)
- pyr-742: TypeScript diagnostics found during pyr-wru (P3)

## Decisions Made
None. The skeleton's Step 4 (assignTypeTo* investigation) was completed — they stay in closure due to expression evaluation entanglement. This was anticipated by the skeleton. Everything else this session was analysis that produced no code.

## Context for Next Session

### Status
pyr-yay is claimed and active. Baseline tests pass (56 suites, 2344 tests). LSP is warm. SRE was done last session. This session completed investigation and pattern study but **zero extraction code was written**. The session was wasted on over-analysis and a failed agent delegation attempt.

### What the next session needs to do
Pick up at Step 4 of pyr-yay: create typeAssignment.ts and extract functions. **Just start writing.** The pattern is clear (see specialForms.ts). The TypeScript compiler catches errors. Don't read 3000 lines before writing — read one function, transform it, write it, repeat.

### Extraction pattern (verified)
- `import * as typeAssignment from './typeAssignment'` in typeEvaluator.ts
- Functions take `evaluator: TypeEvaluator` as first param, plus `registry: TypeRegistry` and `state: TypeEvaluatorState` as needed
- Interface-exposed functions get delegation lambdas in the evaluator object (line 25083)
- Never import from typeEvaluator.ts — only from typeEvaluatorTypes.ts, types.ts, typeUtils.ts, etc.
- Calls to interface methods become `evaluator.printType(...)`, `evaluator.printSrcDestTypes(...)`, etc.
- `evaluatorInterface` in external module calls becomes `evaluator`
- Intra-module calls pass evaluator/registry/state through

### Functions to extract (Steps 4+5 — tightly coupled, extract together)
Core cluster: assignType (21108), assignClass (20471), assignClassToSelf (20667), assignClassWithTypeArgs (20841), assignTypeArgs (20979), assignFunction (23031), assignParam (22850), adjustSourceParamDetailsForDestVariadic (22934), assignRecursiveTypeAliasToSelf (21941), convertToTypeFormType (21979), assignFromUnionType (22031), assignToUnionType (22531), assignConditionalTypeToTypeVar (22721), setConstraintsForFreeTypeVars (22314).

Compatibility helpers: isSpecialFormClass (22304), isTypeSubsumedByOtherType (22331), isProperSubtype (22356), isTypeComparable (22401).

### Key closure dependency mappings (verified via LSP)
- assignType: uses registry (functionClass, strClass, noneTypeClass, methodClass). Calls evaluator.printType, evaluator.printSrcDestTypes, evaluator.makeTopLevelTypeVarsConcrete, evaluator.getCallbackProtocolType, evaluator.getBoundMagicMethod. Calls assignTypeVar from constraintSolver.ts, createFunctionFromConstructor from constructors.ts, assignClassToProtocol/assignModuleToProtocol from protocols.ts.
- assignClass: uses registry (mappingClass, strClass, dictClass). Calls evaluator.printType. Calls assignTypedDictToTypedDict/getTypedDictMappingEquivalent/getTypedDictDictEquivalent from typedDicts.ts, assignClassToProtocol from protocols.ts.
- assignClassToSelf: uses state.assignClassToSelfStack. Calls evaluator.getEffectiveTypeOfSymbol, evaluator.getTypeOfMember, evaluator.getTypeClassType, evaluator.isFinalVariableDeclaration, evaluator.inferVarianceForClass. Calls assignProperty from properties.ts.
- assignTypeArgs: uses state.assignClassToSelfStack. Calls evaluator.printSrcDestTypes, evaluator.inferVarianceForClass.
- assignFunction: uses registry (boolClass). Calls evaluator.solveAndApplyConstraints, evaluator.printType. Needs local getEffectiveReturnType helper.
- assignParam: calls evaluator.solveAndApplyConstraints, evaluator.printType.
- assignToUnionType: calls evaluator.printSrcDestTypes, evaluator.makeTopLevelTypeVarsConcrete, evaluator.stripLiteralValue.
- isTypeSubsumedByOtherType: calls evaluator.makeTopLevelTypeVarsConcrete.

### Delegation strategy for typeEvaluator.ts
- Keep local delegate functions for `assignType` (~20 call sites in remaining closure) and `convertToTypeFormType` (1 call site at line 856). These have same signature as before but body delegates to typeAssignment.xxx(evaluatorInterface, registry, state, ...).
- Interface object entries (line 25083+) for assignType, assignClassToSelf, assignTypeArgs, isTypeComparable, isTypeSubsumedByOtherType, isSpecialFormClass: replace shorthand with delegation lambdas, or let local delegate handle it.

### Critical failure to avoid
Do not over-analyze. Do not spawn agents. Read a function, write the transformed version, move on. Trust the compiler.
