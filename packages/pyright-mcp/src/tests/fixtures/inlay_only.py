# Pure script — no classes, no functions worth refs on.
# Fixture for the empty-suppression regression: codeLens set may be empty
# but inlay hints (inferred types for unannotated locals) must still flow.
counter = 0
total = 100
name = "hello"
flag = True
